server:
  port: 8086

spring:
  application:
    name: inventory-service
  profiles:
    active: ${profile}
  jpa:
    database: mysql
    hibernate.ddl-auto: update
    show-sql: true
  output:
    ansi:
      enabled: always
  config:
    import: optional:configserver:http://config-server:8888

